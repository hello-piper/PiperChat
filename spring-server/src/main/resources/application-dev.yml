spring:
  datasource:
    url: jdbc:mysql://localhost:3306/im
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    password: "123456"
    timeout: 3000
    pool:
      max-active: 8
      max-wait: 3000
      max-idle: 8
      min-idle: 0
